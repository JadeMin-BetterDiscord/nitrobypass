{
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsx ./scripts/build.ts",
		"build:publish": "npm run build PUBLISH",
		"build:test": "npm run build TEST",
		"publish": "npm run build PUBLISH && npm run build BD"
	},
	"dependencies": {
		"discord-api-types": "^0.37.56"
	},
	"devDependencies": {
		"@types/betterdiscord": "github:zerthox/betterdiscord-types",
		"@types/node": "^20.5.9",
		"esbuild": "^0.19.2",
		"esbuild-plugin-import-glob": "^0.1.1",
		"tsx": "^3.12.8"
	}
}