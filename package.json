{
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsx ./scripts/build.ts",
		"build:publish": "pnpm run build PUBLISH",
		"build:test": "pnpm run build TEST",
		"inject": "pnpm run build BD"
	},
	"dependencies": {
		"discord-api-types": "^0.37.67",
		"esbuild": "^0.19.11",
		"tsx": "^4.7.0"
	},
	"devDependencies": {
		"@types/betterdiscord": "github:zerthox/betterdiscord-types",
		"@types/node": "^20.10.6"
	}
}